void swapAlternate(int *arr, int size)
{
    if(size == 0)
    {
         return;
    }
    int n =size/ 2;
    int i = 0;
        while( n > 0 ){
            int temp = arr[i];
            arr[i] = arr[i+1];
            arr[i+1] = temp;
            i +=2;
            n--;
        }
}
